{
  "name": "culinarpg",
  "version": "2.1.0",
  "description": "CulinaRPG — le Solo Leveling de la cuisine. Monte en niveau en cuisinant.",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "postinstall": "prisma generate",
    "db:push": "prisma db push",
    "db:generate": "prisma generate",
    "db:seed": "node prisma/seed.js",
    "setup": "npm install && prisma db push && node prisma/seed.js",
    "setup:ps": "npm install ; prisma db push ; node prisma/seed.js",
    "images": "node lib/images.js",
    "render-build": "npm ci && npx prisma generate && npx prisma db push && node prisma/seed.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "axios": "^1.12.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "prisma": "^6.19.3",
    "stripe": "^22.6.2"
  }
}
